% DESARROLLO DEL PROYECTO Y RESULTADOS

\section{Desarrollo del proyecto y resultados}



\subsection{Planteamiento del problema}

La calidad del aire constituye un problema técnico, sanitario y de gestión pública cuya complejidad supera la mera medición puntual de contaminantes. La evidencia reciente muestra que la exposición prolongada a contaminantes atmosféricos sigue asociándose a una carga global elevada de enfermedad y mortalidad: distintos antecedentes reportan alrededor de siete millones de muertes a escala mundial vinculadas a la exposición prolongada a material particulado y otros contaminantes del aire \cite[p.~1]{ART07}, mientras que otros trabajos sitúan en 4.2 millones las muertes prematuras asociadas a contaminación atmosférica exterior \cite[p.~1]{ART04}. Esta magnitud confirma que el problema no se limita a la vigilancia ambiental, sino que exige mecanismos de evaluación y alerta que puedan traducir datos técnicos en decisiones operativas oportunas para reducir exposición y riesgo.

Dentro de ese escenario, el índice de calidad del aire (AQI) se ha consolidado como uno de los principales instrumentos para interpretar el estado del aire y comunicarlo a autoridades y ciudadanía. Sin embargo, la literatura especializada también señala que persisten diferencias relevantes en contaminantes considerados, métodos de cálculo, escalas de agregación y criterios de interpretación entre modelos de AQI. La revisión crítica de modelos AQI desarrollados entre 1960 y 2021 concluye, de forma explícita, que todavía se requiere un modelo más confiable, extensible y comparable para apoyar programas estratégicos de abatimiento de la contaminación y preservar la salud pública \cite[p.~1]{ART01}. Esta necesidad se vuelve más evidente en contextos urbanos con alta presión ambiental; por ejemplo, en dicha revisión se reporta que Delhi registró en 2019 una concentración media diaria de PM$_{2.5}$ de 98.6~\(\mu g/m^3\), equivalente a diez veces la recomendación de la OMS \cite[p.~2]{ART01}. En consecuencia, el problema no es únicamente captar datos, sino convertirlos en evaluaciones consistentes, comparables y útiles para la toma de decisiones.

En respuesta a esta necesidad, los trabajos recientes han incorporado lógica difusa, IoT y arquitecturas distribuidas para clasificación, monitoreo y predicción de calidad del aire. No obstante, el análisis de antecedentes muestra que estos avances siguen siendo parciales y heterogéneos. Existen propuestas basadas en datasets abiertos que reconocen la necesidad de ampliar su validación a otras localizaciones para reducir sesgo geográfico \cite[p.~7]{ART04}; otras implementaciones priorizan el monitoreo y control local, pero declaran que no generaron ni analizaron datasets para validar su comportamiento de manera reproducible \cite[p.~10]{ART05}; y trabajos centrados en redes WSN mejoran eficiencia de red, retardo o entrega de paquetes, pero concentran la optimización en la infraestructura de transmisión más que en la validación comparable de modelos predictivos sobre datos reutilizables \cite[pp.~1,4]{ART08}. Esta diversidad confirma que el campo está activo, pero también que la trazabilidad del dato, la comparabilidad de evaluación y la transferencia de resultados entre contextos siguen siendo insuficientes.

La revisión sistemática desarrollada en este trabajo refuerza esa misma lectura. En el corpus analizado predominan estudios sustentados en adquisición directa por sensores, mientras la utilización exclusiva de datos públicos es excepcional; además, la mayor parte de las propuestas trabaja con conjuntos reducidos de contaminantes y con énfasis mayor en clasificación o alertamiento, no en predicción robusta y externamente comparable. Los estudios orientados a predicción, aunque técnicamente prometedores, continúan concentrados en escenarios acotados y con validaciones parciales. Por ejemplo, algunos trabajos combinan datos de pocos sitios urbanos y rurales con validación adicional sobre subconjuntos de datasets de referencia \cite[pp.~3--4]{A135}, \cite[p.~2]{A144}; otros reportan predicción a 24 horas, pero dentro de entornos interiores específicos \cite[p.~1]{A157}; y propuestas recientes con alto ajuste estadístico plantean como trabajo futuro la colaboración entre múltiples ciudades, lo que evidencia que la validación interurbana sigue sin resolverse de manera sólida \cite[p.~5]{A028}. En contraste, casos como \cite[p.~2]{A084} muestran el valor de entrenar y depurar modelos neuro-difusos sobre datos multi-localización del mundo real, pero este tipo de aproximación todavía no representa la práctica dominante.

En funciones metodológicas y de ingeniería, el problema central radica en la ausencia de una solución que integre, en un mismo marco reproducible, cuatro componentes que suelen aparecer desacoplados: una fuente de datos abiertos trazable y suficientemente robusta, un proceso de preparación y control de calidad de datos documentado, un núcleo de inferencia difusa auditable para interpretar riesgo, y una validación comparable que permita verificar estabilidad del desempeño fuera del contexto inicial. Cuando alguno de estos componentes falta, el resultado puede ser útil como prototipo local, pero no como base confiable para una solución transferible y reutilizable en otros escenarios urbanos.

Con base en ello, el problema que aborda este TFM se define como la falta de un módulo de monitoreo de calidad del aire que, apoyado en datos abiertos de redes de monitoreo ambiental, permita evaluar riesgo y generar alertas mediante inferencia difusa bajo un esquema técnicamente trazable, reproducible y con capacidad de validación en escenarios urbanos. Esta formulación se alinea de manera directa con los objetivos del trabajo: caracterizar la evidencia disponible, diseñar una arquitectura funcional coherente con los vacíos detectados, implementar el módulo propuesto e introducir una validación que considere la utilidad práctica del modelo como sistema de apoyo a la decisión.


\subsection{Metodología}

\subsubsection{Enfoque y tipo de investigación}
El presente trabajo se enmarca en una investigación aplicada orientada al desarrollo de una solución tecnológica para un problema real. El enfoque metodológico combina una revisión sistemática de la literatura, utilizada para fundamentar el problema y derivar criterios de diseño, y una lógica de investigación basada en diseño de artefacto, consistente con los principios de \emph{Design Science Research}, donde el conocimiento se materializa mediante la definición, construcción y evaluación de una solución útil frente a una necesidad identificada \cite[pp.~75--77]{METDSR01}, \cite[pp.~46,55--56]{METDSRM01}. La validación del módulo se formula como una evaluación empírica del artefacto centrada en la utilidad, la calidad y el desempeño de la solución desarrollada.

\subsubsection{Diseño metodológico del estudio}
El estudio se organiza en fases metodológicas claramente definidas para garantizar coherencia y trazabilidad entre los objetivos planteados y las actividades desarrolladas. La secuencia articula el proceso de revisión sistemática con la lógica propuesta por DSRM para investigación basada en diseño \cite[pp.~46,71]{METDSRM01}. La primera fase cumple la función de fundamentación y delimitación del problema. Las fases posteriores se orientan a la definición de la solución, la construcción del artefacto, su demostración funcional y su evaluación. Las fases del estudio son las siguientes:

\begin{enumerate}
  \item Revisión sistemática del estado del arte.
  \item Diseño del módulo de monitoreo.
  \item Implementación del módulo propuesto.
  \item Validación del módulo propuesto.
\end{enumerate}

La relación con DSRM se utiliza aquí como marco de organización del estudio. La fase 1, basada en revisión sistemática, cumple la función de identificar y justificar el problema y aporta la base para definir los objetivos de la solución. La fase 2 corresponde a la definición de esos objetivos y al diseño conceptual del artefacto. La fase 3 corresponde a la construcción del artefacto e incorpora una demostración funcional inicial de su operabilidad. La fase 4 corresponde a la evaluación del módulo implementado. El paso de comunicación de DSRM se concreta en la memoria del TFM y en el manuscrito derivado de la revisión sistemática \cite[pp.~51--56]{METDSRM01}.

\subsubsection{1: Revisión sistemática del estado del arte}

La revisión sistemática constituye la primera fase metodológica del TFM. Su función fue identificar el problema, caracterizar el estado del arte reciente y derivar criterios de diseño para el artefacto desarrollado en las fases posteriores. Aunque la revisión forma parte del presente trabajo, su desarrollo completo se consolidó en un manuscrito independiente enviado a evaluación. En esta memoria se conserva únicamente el nivel de detalle necesario para explicar su papel metodológico; la síntesis de hallazgos resultantes se presenta en el Capítulo 3.

El protocolo de revisión se estructuró mediante el modelo PICo y la guía PRISMA 2020 \cite{prisma2020}, siguiendo además las recomendaciones de Kitchenham para revisiones en ingeniería de software y sistemas \cite{kitchenham2007}. La delimitación PICo permitió definir con precisión el fenómeno de interés, el enfoque metodológico y el contexto de aplicación, manteniendo consistencia con el problema del TFM.

\input{assets/tablas/02_cuerpo/05_desarrollo/pico_air_quality.tex}

A partir de esta delimitación se formuló la pregunta de revisión: ¿cómo se utilizan los sistemas de inferencia basados en lógica difusa en plataformas de monitoreo en tiempo real para evaluar la calidad del aire urbano y los riesgos asociados? Esta pregunta se operacionalizó mediante tres directrices analíticas: variables y contaminantes utilizados, criterios de diseño de los modelos difusos y características de las plataformas de monitoreo en tiempo real.

La búsqueda bibliográfica se ejecutó en Scopus, Web of Science e IEEE Xplore. Se restringió a publicaciones en inglés entre 2021 y 2026 y a documentos arbitrados con relación directa con monitoreo de calidad del aire, lógica difusa y operación en tiempo real. La construcción de la cadena de búsqueda siguió la estructura PICo y combinó términos del dominio atmosférico, del enfoque difuso y del contexto de plataforma mediante bloques lógicos equivalentes entre bases. El objetivo de esta fase fue recuperar un conjunto suficientemente amplio para caracterizar el campo, pero lo bastante acotado para sostener una evaluación detallada y trazable.

La selección de estudios siguió el flujo PRISMA 2020, con fases de identificación, depuración de duplicados, cribado por título y resumen, recuperación de texto completo, elegibilidad e inclusión final. La elegibilidad se definió con base en la relación efectiva del estudio con tres dimensiones: datos y variables de calidad del aire, uso central de lógica difusa y presencia de plataforma o sistema de monitoreo. El corpus final quedó constituido por 19 estudios primarios. Los artefactos metodológicos y de respaldo de esta fase se consolidaron en el manuscrito del SLR y en su repositorio asociado.

\subsubsection{Evaluación de Calidad}
La evaluación de calidad se diseñó para valorar la solidez metodológica de los estudios incluidos y para apoyar la interpretación posterior de la evidencia. El instrumento se construyó a partir del bloque cuantitativo propuesto por Kitchenham \cite{kitchenham2007}, seleccionando únicamente ítems pertinentes para la pregunta de revisión, el tipo de evidencia reportada y el dominio analizado. El resultado fue un checklist de 30 ítems, organizado en un núcleo común y componentes específicos según subtipo de estudio cuantitativo.

Cada estudio se clasificó previamente según su naturaleza metodológica y se evaluó únicamente con los ítems aplicables. La codificación utilizó las categorías \texttt{Yes}, \texttt{Partial}, \texttt{No}, \texttt{Unclear} y \texttt{N/A}, con respaldo por página para cada decisión. La puntuación agregada se empleó para distinguir evidencia de calidad alta, media o baja, pero no como criterio de exclusión automática. Su función fue orientar la lectura crítica de los hallazgos y detectar posibles sesgos de diseño, ejecución, análisis o reporte.

\subsubsection{Extracción de datos}
La extracción de datos se definió antes del análisis y se orientó a responder de forma directa la pregunta principal y las tres directrices de la revisión. La matriz final integró metadatos bibliográficos, variables y contaminantes, características del enfoque difuso, arquitectura y operación de plataforma, resultados reportados y trazabilidad metodológica. Antes de la extracción definitiva se realizó un piloto para estabilizar definiciones operacionales, reducir ambigüedad semántica y eliminar campos redundantes. Como resultado de ese ajuste, la matriz final quedó estructurada en 48 columnas analíticas.

La extracción se ejecutó artículo por artículo, con un catálogo controlado para variables categóricas y con registro explícito de evidencia por página para cada afirmación analítica. Esta decisión permitió mantener una relación verificable entre síntesis e información de origen. La consistencia de la matriz se comprobó mediante controles estructurales, normalización semántica y auditoría cruzada contra los PDFs de los estudios incluidos.

\subsubsection{Análisis y Síntesis de Datos}
El análisis se desarrolló en una secuencia de preparación, descripción, identificación de patrones, construcción de perfiles e integración narrativa. En la fase de preparación se normalizaron categorías y se consolidó la matriz de extracción. En la fase descriptiva se calcularon frecuencias y distribuciones por dimensión analítica. En la fase relacional se emplearon tablas cruzadas y matrices de coocurrencia para identificar configuraciones recurrentes entre variables, modelos difusos y plataformas. Posteriormente, estos patrones se sintetizaron en perfiles comparables de diseño y operación.

La implementación analítica se realizó con Python, principalmente mediante \texttt{pandas}, \texttt{matplotlib} y \texttt{seaborn}. Este flujo permitió mantener consistencia entre procesamiento, visualización y redacción de resultados. La salida sustantiva de esta fase se recoge en el Capítulo 3, donde se presentan los hallazgos del estado del arte y sus implicaciones para el diseño del módulo propuesto.




\subsubsection{2: Diseño del módulo de monitoreo}
Esta fase se inscribe en el segundo paso de DSRM, \emph{define the objectives of a solution}, y recoge además la parte inicial del tercer paso, \emph{design and development}, en lo relativo a la formulación conceptual del artefacto \cite[pp.~52--55]{METDSRM01}. Su función es establecer, con base en la fase 1, qué debe resolver el módulo y bajo qué condiciones debe hacerlo. Esta fase convierte los hallazgos del estado del arte en requisitos explícitos de solución.

El trabajo de diseño se desarrolla sobre cuatro decisiones principales:
\begin{enumerate}
\item \textbf{Delimitación del alcance funcional del módulo}. En esta decisión se distinguen las funciones que forman parte del artefacto: adquisición de datos, consolidación del estado de calidad del aire, evaluación difusa del riesgo y generación de alertas.
\item \textbf{Definición del flujo lógico de procesamiento}. En este punto se identifica el recorrido de los datos desde la fuente de entrada hasta la salida final y se fijan los puntos donde el sistema debe conservar trazabilidad documental.
\item \textbf{Precisión de la estructura de entradas y salidas}. Esta decisión incluye la definición de contaminantes, covariables, variables derivadas y categorías de riesgo que deberán integrarse en el módulo.
\item \textbf{Selección del esquema de inferencia y de los criterios de diseño}. Aquí se determina el tipo de inferencia difusa y las condiciones de diseño del artefacto, de modo que la propuesta responda al objetivo del TFM y a los vacíos identificados en comparabilidad, auditabilidad e integración operativa.
\end{enumerate}

Como producto de esta fase se obtiene una especificación de diseño. Esa especificación debe dejar definidos el alcance del artefacto, la arquitectura funcional, el papel de la fuente de datos, el criterio de construcción de la salida y las condiciones que guiarán la implementación y la validación posterior. Su función dentro del estudio es reducir ambigüedad entre la evidencia revisada y el desarrollo técnico del módulo \cite[pp.~84--85]{METDSR01}.

\subsubsection{3: Implementación del módulo propuesto}
Esta fase desarrolla el núcleo del tercer paso de DSRM, \emph{design and development}, y enlaza con el cuarto paso, \emph{demonstration}, mediante una verificación funcional inicial del artefacto \cite[pp.~55--56]{METDSRM01}. Su función es convertir la especificación definida en la fase 2 en un módulo ejecutable. En esta etapa se construyen e integran los componentes del sistema bajo un criterio de continuidad operativa y reproducibilidad técnica.

La implementación comprende las siguientes tareas:
\begin{enumerate}
\item \textbf{Selección y configuración de tecnologías de soporte}. Esta tarea fija la base técnica sobre la que se ejecutará el módulo.
\item \textbf{Construcción del mecanismo de adquisición y preparación de datos}. Aquí se implementan las funciones de consulta, transformación, control de calidad y estructuración de la entrada.
\item \textbf{Codificación del núcleo de inferencia}. En esta tarea se materializan las variables, reglas y salidas del componente difuso.
\item \textbf{Integración del flujo completo de ejecución}. En este punto se enlazan adquisición, procesamiento, inferencia y salida dentro de un único recorrido funcional.
\item \textbf{Definición de reglas operativas de ejecución}. Esta tarea incluye tratamiento de errores, registro de metadatos y trazabilidad de la ejecución.
\end{enumerate}

El objetivo metodológico de la fase es disponer de un artefacto funcional cuya ejecución pueda repetirse bajo las mismas condiciones y con resultados consistentes.

La demostración funcional se ubica al cierre de esta fase. Su propósito es comprobar que el módulo ejecuta el recorrido completo previsto en el diseño, desde la recuperación de datos hasta la emisión de una salida interpretable. Esta demostración no sustituye la evaluación formal, pero aporta evidencia de factibilidad técnica y permite identificar fallos de integración antes de la fase de validación \cite[p.~85]{METDSR01}.

\subsubsection{4: Validación del módulo propuesto}
Esta fase corresponde al quinto paso de DSRM, \emph{evaluation} \cite[p.~56]{METDSRM01}. Su propósito es determinar si el módulo satisface los objetivos fijados en la fase 2 y si su comportamiento resulta útil, consistente y técnicamente adecuado para el problema abordado. La validación se organiza como una evaluación empírica del artefacto, orientada a comprobar el comportamiento del sistema bajo condiciones de uso definidas de antemano.

La evaluación se estructura mediante escenarios representativos del funcionamiento esperado del módulo:
\begin{enumerate}
\item \textbf{Condiciones nominales de operación}. Se comprueba el comportamiento del sistema cuando los datos se encuentran dentro de rangos esperados.
\item \textbf{Situaciones cercanas o superiores a umbrales de alerta}. Se verifica la coherencia entre las condiciones observadas y la respuesta de riesgo emitida por el módulo.
\item \textbf{Casos con variaciones en completitud o calidad del dato}. Se observa la estabilidad del sistema ante faltantes, cobertura parcial o disponibilidad incompleta de contaminantes.
\end{enumerate}

Cada escenario se ejecuta sobre una ventana temporal explícita y con documentación de la fuente utilizada, de la cobertura efectiva de datos y de las reglas de selección del conjunto evaluado. Este procedimiento es necesario porque la propuesta depende de datos abiertos y, por tanto, la validez de la evaluación depende tanto del motor difuso como de la calidad y trazabilidad de la entrada.

La validación considera cuatro dimensiones de análisis:
\begin{enumerate}
\item \textbf{Completitud funcional del pipeline}. Se verifica la capacidad del sistema para recorrer todas sus etapas y producir una salida.
\item \textbf{Coherencia entre condiciones observadas y respuesta del módulo}. Se analiza la correspondencia entre la entrada y la clasificación o alerta emitida.
\item \textbf{Desempeño operativo}. Se registra el tiempo de respuesta y la estabilidad básica de ejecución.
\item \textbf{Reproducibilidad}. Se comprueba la estabilidad de la salida cuando se repiten las mismas condiciones de entrada y procesamiento.
\end{enumerate}

El resultado esperado de esta fase es una base empírica suficiente para juzgar el comportamiento del artefacto e introducir ajustes cuando sea necesario \cite[pp.~86--87]{METDSR01}.

\subsubsection{Consideraciones finales de la metodología}
La secuencia metodológica del TFM se organizó mediante una planificación temporal por fases, articulando revisión sistemática, diseño del módulo, implementación, validación y redacción final. La Figura~\ref{fig:roadmap_tfm} resume esta organización entre mediados de octubre de 2025 y mediados de abril de 2026, identificando la duración relativa de cada bloque, sus solapes principales y los hitos del proceso.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{metodología/roadmap_tfm.png}
\caption{Roadmap temporal del TFM por fases, actividades e hitos principales. Elaboración propia.}
\label{fig:roadmap_tfm}
\end{figure}

Este esquema cumple una función metodológica de trazabilidad. Sitúa cada fase dentro del desarrollo global del trabajo, identifica dependencias entre actividades y hace visible la superposición parcial entre síntesis, diseño, construcción y cierre documental. En el tramo final del estudio, esta planificación también sirve como base para interpretar el alcance efectivo del trabajo, sus limitaciones y las extensiones posibles del módulo propuesto.



\subsection{Desarrollo del proyecto}

\subsubsection{Propuesta del módulo}
La propuesta técnica del TFM se materializó en un prototipo ejecutable denominado \textit{AQRisk}. El artefacto implementa un módulo híbrido para monitoreo de calidad del aire, evaluación de riesgo y generación de alertas sobre datos abiertos. Su diseño conserva una base normativa explícita para el cálculo del estado del aire y añade una capa de inferencia difusa para ajustar la severidad operativa del episodio.

La decisión central del diseño fue separar dos problemas. El primero consiste en consolidar el estado base del aire con una referencia regulatoria trazable. El segundo consiste en interpretar ese estado cuando aparecen concurrencia entre contaminantes, persistencia temporal o condiciones ambientales de contexto. Esta separación mantiene comparabilidad externa y deja trazable el aporte del módulo en la parte de interpretación y alertamiento.

\subsubsection{Arquitectura funcional del módulo}
El prototipo implementado se organiza en cinco capas funcionales encadenadas. La Figura~\ref{fig:capas_modulo} resume esta arquitectura.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/fig01_capas_modulo.png}
\caption{Arquitectura funcional implementada del módulo AQRisk. Elaboración propia.}
\label{fig:capas_modulo}
\end{figure}

\begin{enumerate}
\item \textbf{Consolidación normativa}. Recupera las series horarias, calcula subíndices por contaminante y consolida el AQI global.
\item \textbf{Variables auxiliares}. Deriva persistencia temporal y concurrencia multi-contaminante a partir del estado base calculado.
\item \textbf{Inferencia difusa principal}. Aplica la base principal de 54 reglas sobre AQI base, concurrencia y persistencia.
\item \textbf{Ajuste contextual}. Modula la salida principal mediante temperatura y humedad cuando esas variables están disponibles y el episodio ya se encuentra comprometido.
\item \textbf{Alertamiento y salida}. Genera la clasificación final, compone la alerta interpretable y serializa el resultado en formato JSON.
\end{enumerate}

La arquitectura por capas aporta explicabilidad técnica. Cada bloque resuelve una función específica y conserva trazabilidad del paso anterior. Esta organización facilita la verificación del comportamiento del sistema y evita mezclar cálculo normativo, lógica difusa y composición de la salida dentro de una sola etapa.

\subsubsection{Arquitectura de software y flujo de ejecución}
La implementación se realizó en Python mediante una arquitectura modular monoproceso. La estructura del paquete separa modelos de dominio, ingesta, procesamiento, cálculo AQI, motor difuso, alertamiento, orquestación e interfaz de línea de comandos. Esta decisión reduce complejidad accidental y deja preparada una migración posterior a servicios independientes si el trabajo la requiere.

El flujo de ejecución del prototipo se presenta en la Figura~\ref{fig:pipeline_prototipo}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{propuesta/fig02_pipeline_prototipo.png}
\caption{Flujo de ejecución del prototipo implementado. Elaboración propia.}
\label{fig:pipeline_prototipo}
\end{figure}

El pipeline resuelve la configuración de ejecución, consulta sensores por \textit{location\_id}, recupera series horarias recientes, normaliza datos, calcula cobertura, construye el AQI base, deriva concurrencia y persistencia, ejecuta el motor difuso, aplica la capa contextual cuando corresponde y genera una salida JSON trazable. Esta salida está preparada para exponer el módulo por CLI, por contenedor Docker y, en una etapa posterior, por API o interfaz web.

\subsubsection{Fuente de datos, variables y criterios normativos}
La fuente operativa principal del prototipo es \textit{OpenAQ}, utilizada como plataforma abierta de consulta programática de mediciones y metadatos de estaciones \cite{DATA_OPENAQ}. El módulo recupera mediciones por estación, sensor y ventana temporal. La fuente se usa como repositorio de observaciones y no como proveedor de un índice final ya resuelto.

La capa normativa del módulo se apoya de forma explícita en la tabla vigente de \textit{AQI Breakpoints} de \textit{EPA/AQS} \cite{DATA_EPA_AQS_BREAKPOINTS}. A partir de esa referencia se implementaron los contaminantes criterio PM$_{2.5}$, PM$_{10}$, CO, NO$_2$, O$_3$ y SO$_2$, junto con sus ventanas regulatorias y categorías del índice. La versión actual del prototipo utiliza:
\begin{enumerate}
\item PM$_{2.5}$ con promedio de 24 horas.
\item PM$_{10}$ con promedio de 24 horas.
\item CO con promedio de 8 horas.
\item NO$_2$ con valor horario.
\item O$_3$ con comparación entre ventanas de 8 horas y 1 hora, según tramo aplicable.
\item SO$_2$ con comparación entre valor horario y promedio de 24 horas.
\end{enumerate}

El tratamiento de ventanas parciales se resuelve mediante un umbral mínimo de cobertura del 80\%. Cuando la ventana no está completa, el módulo admite cálculo si la cobertura supera ese umbral y deja registro de la condición aplicada. Esta decisión conserva utilidad operativa sobre datos abiertos sin ocultar la completitud efectiva de la serie.

El prototipo admite además temperatura y humedad como variables de contexto. Estas variables no participan en la consolidación del AQI y tampoco sustituyen a los contaminantes criterio. Su función es modular la salida del riesgo cuando ya existe un episodio comprometido. La variable CO$_2$ no se incorporó al núcleo porque su uso en los antecedentes revisados se concentra en calidad de aire interior y escenarios IAQ, no en el núcleo regulatorio urbano \cite{A135,A165,A167}.

Sobre esa base, el módulo deriva las siguientes variables internas:
\begin{itemize}
\item \textbf{AQI global}, como estado base consolidado.
\item \textbf{Contaminante dominante}, para identificar la variable que gobierna el episodio.
\item \textbf{Persistencia temporal}, calculada sobre estados AQI sucesivos del propio módulo.
\item \textbf{Concurrencia}, definida como cercanía entre varios subíndices respecto del dominante.
\item \textbf{Cobertura global}, como indicador de completitud de la entrada efectiva.
\end{itemize}

La salida final del prototipo conserva la taxonomía EPA: \textit{Good}, \textit{Moderate}, \textit{Unhealthy for Sensitive Groups}, \textit{Unhealthy}, \textit{Very Unhealthy} y \textit{Hazardous}. El resultado se expresa junto con la alerta textual, el AQI base y la lista de reglas activadas.

\subsubsection{Núcleo de evaluación y motor difuso}
El núcleo IA del prototipo se organiza en dos niveles de decisión. El primero es determinista y produce el estado base del aire. El segundo es difuso y ajusta ese estado en función de variables agregadas. La Figura~\ref{fig:estructura_decision} resume esta estructura.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{propuesta/fig03_estructura_decision.png}
\caption{Estructura de decisión implementada en el núcleo IA del módulo. Elaboración propia.}
\label{fig:estructura_decision}
\end{figure}

El motor principal se implementó con inferencia \textit{Mamdani}. La elección respondió a interpretabilidad, auditabilidad y control directo sobre las funciones de pertenencia y la base de reglas. La malla principal quedó definida sobre tres variables:
\begin{enumerate}
\item \textbf{AQI base} con seis categorías EPA.
\item \textbf{Concurrencia} con tres niveles: baja, media y alta.
\item \textbf{Persistencia} con tres niveles: baja, media y alta.
\end{enumerate}

Esta combinación produce una base completa de 54 reglas. La Tabla~\ref{tab:reglas_principales_resumen} resume la lógica por bloques y el detalle completo se presenta en el Anexo~\ref{ane:anexo_reglas_principales}, Tabla~\ref{tab:anexo_reglas_principales_54}. La decisión de utilizar una malla completa y no una base reducida deriva de dos criterios: exhaustividad de cobertura y defensa metodológica frente a tribunal. También es consistente con antecedentes que documentan bases explícitas de 36 y 64 reglas cuando el sistema difuso trabaja sobre entradas instrumentales directas \cite{A114,A165,A167}.

\input{assets/tablas/02_cuerpo/05_desarrollo/reglas_principales_resumen.tex}

La capa contextual se resolvió con una base independiente de 9 reglas sobre temperatura y humedad. Su papel es escalar una categoría cuando el riesgo base ya se encuentra comprometido y el contexto ambiental resulta menos favorable. La Tabla~\ref{tab:reglas_contextuales} presenta esta base.

\input{assets/tablas/02_cuerpo/05_desarrollo/reglas_contextuales.tex}

El mecanismo de inferencia utiliza implicación por mínimo, agregación por máximo y defuzzificación por centroide discreto en el rango 0--500. La etiqueta final se obtiene mediante bandas equivalentes a la taxonomía EPA. Este diseño permite explicar qué regla se activó, sobre qué entrada se apoyó y cómo se obtuvo la categoría final.

\subsubsection{Implementación del prototipo}
El prototipo se implementó en Python 3.12 bajo el paquete \texttt{aqrisk}. La estructura del software separa las responsabilidades principales:
\begin{itemize}
\item \texttt{domain}: modelos de datos y estructuras del recorrido.
\item \texttt{ingestion}: cliente de OpenAQ y construcción de series por sensor.
\item \texttt{processing}: cobertura, persistencia, concurrencia y ajuste contextual.
\item \texttt{aqi}: cálculo del AQI base con ventanas regulatorias.
\item \texttt{fuzzy}: funciones de pertenencia, matriz de reglas y motor Mamdani.
\item \texttt{alerting}: composición de la alerta y de la cautela operativa.
\item \texttt{application}: orquestación del pipeline.
\item \texttt{interfaces}: interfaz de línea de comandos.
\end{itemize}

La ejecución local puede realizarse directamente en Python o mediante Docker. Sobre ese núcleo se implementó además una API HTTP y una interfaz web orientada a control, visualización, explicabilidad y revisión histórica de corridas. La interfaz permite seleccionar entradas, consultar escenarios, visualizar subíndices, reglas activadas, curvas de pertenencia y comparación entre ejecuciones. La gestión de usuarios, roles y administración todavía no forma parte del alcance actual.

\subsubsection{Estrategia de verificación del artefacto}
La verificación actual del prototipo se centró en completitud del pipeline, coherencia de la salida, trazabilidad de reglas y estabilidad básica de ejecución. Para ello se implementaron pruebas de humo sobre el modo sintético y sobre la validación mínima del modo \textit{OpenAQ}. También se ejecutó un escenario \textit{mock} con cobertura total y disponibilidad completa de contaminantes criterio para comprobar la integración de las cinco capas.

\subsection{Resultados}
La construcción del prototipo permitió cerrar un primer artefacto funcional alineado con la metodología del TFM. El módulo ya ejecuta ingestión, normalización, control de cobertura, cálculo normativo del AQI, derivación de persistencia y concurrencia, inferencia Mamdani, ajuste contextual y composición de la alerta. Sobre este núcleo se integró una capa web de supervisión que consume la API del sistema y expone paneles de dashboard, trazabilidad, explicabilidad y evaluación.

La Tabla~\ref{tab:validacion_prototipo_actual} resume la verificación funcional actual.

\input{assets/tablas/02_cuerpo/05_desarrollo/validacion_prototipo_actual.tex}

Las pruebas automatizadas validaron dos aspectos básicos: la ejecución completa del escenario sintético y la exigencia de clave para el modo \textit{OpenAQ}. Ambas finalizaron correctamente en el entorno de desarrollo. En el escenario controlado \textit{urban\_escalation}, el módulo procesó seis contaminantes criterio y variables de contexto, obtuvo un AQI global de 112, identificó O$_3$ como contaminante dominante, activó la regla \texttt{R\_unhealthy\_sensitive\_groups\_low\_medium} y escaló la salida final a \textit{Unhealthy} mediante \texttt{CTX\_high\_high}. Esa corrida se conservó como referencia controlada porque los casos reales consultados no produjeron una alerta equivalente ni un escalado contextual visible.

La validación visual combinó corridas reales y controladas. Las corridas reales se usaron para verificar comportamiento con datos vivos, cobertura efectiva y diferencias entre estaciones. Las corridas controladas se usaron para mostrar condiciones que el muestreo real no ofreció en esta iteración: soporte completo de contaminantes criterio, alerta de mayor severidad y ajuste contextual activo. Para la parte real se seleccionaron tres ubicaciones con comportamiento útil y estable en la ventana de 24 horas consultada: \textit{Hospital General de México} (\texttt{location\_id=1134}), \textit{Parque O'Higgins} en Santiago (\texttt{25}) y \textit{SAN BORJA} en Lima (\texttt{2402}). La primera aporta un perfil dominado por contaminantes gaseosos; las otras dos concentran la señal en material particulado fino y grueso.

El \textit{dashboard} actúa como punto de entrada de esa lectura. La Figura~\ref{fig:ui_dashboard_mock_controlled} muestra la vista inicial con una corrida controlada completa. El bloque superior concentra la operación: selección de modo, escenario o estación, ventana temporal y cobertura mínima. A la derecha, la interfaz resume la corrida actual mediante tarjetas con fuente, entrada seleccionada, contaminante dominante, número de reglas activadas, ajuste contextual e histórico local. Debajo, cuatro indicadores fijan la lectura ejecutiva del episodio: AQI global, categoría base, riesgo final y cobertura. Esa síntesis permite decidir si la corrida requiere una revisión de auditoría en \textit{Trazabilidad}, una lectura interna del motor en \textit{Explicabilidad} o una comparación temporal en \textit{Evaluación}.

Los colores del \textit{dashboard} siguen una convención estable para reducir ambigüedad entre vistas. El azul identifica la base normativa y las magnitudes ligadas al AQI o a la corrida actual. El naranja se reserva para la concurrencia y, en comparaciones históricas, para la corrida de referencia. El rojo representa persistencia o señales de presión acumulada. El verde se usa para la salida difusa final, la cobertura útil y los estados resueltos por la capa contextual. Esta codificación se repite en las gráficas del visor modal, de modo que la semántica visual no cambia al pasar de \textit{Dashboard} a \textit{Explicabilidad} o \textit{Evaluación}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{propuesta/ui_dashboard_mock_controlled.png}
\caption{Vista principal del \textit{dashboard} para el escenario controlado \textit{urban\_escalation}. La pantalla concentra el control de entrada, el resumen ejecutivo de la corrida y los indicadores base del episodio antes de abrir los modales de auditoría, explicabilidad o evaluación. Elaboración propia.}
\label{fig:ui_dashboard_mock_controlled}
\end{figure}

El modal de \textit{Trazabilidad} permitió verificar dos situaciones complementarias. La Figura~\ref{fig:ui_traceability_santiago_real} corresponde a la corrida real de Santiago y deja visibles la ruta de decisión, los parámetros efectivamente soportados y el inventario de sensores recuperado para esa estación. La Figura~\ref{fig:ui_traceability_mock_controlled} usa \textit{urban\_escalation} y muestra el caso controlado con cobertura completa, soporte de los seis contaminantes criterio y activación explícita del ajuste contextual \texttt{CTX\_high\_high}. Con ambas capturas se puede distinguir entre lo que el sistema hace con datos observados y lo que es capaz de hacer cuando la entrada cubre todo el espacio previsto por el diseño.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{propuesta/ui_traceability_santiago_real.png}
\caption{Modal de \textit{Trazabilidad} para la estación \textit{Parque O'Higgins} de Santiago. La interfaz expone la ruta de decisión del artefacto, separa parámetros soportados y no soportados y deja visible el inventario de sensores publicados por la estación, lo que respalda auditabilidad y defensa metodológica del sistema. Elaboración propia.}
\label{fig:ui_traceability_santiago_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{propuesta/ui_traceability_mock_controlled.png}
\caption{Modal de \textit{Trazabilidad} para el escenario controlado \textit{urban\_escalation}. La captura muestra soporte completo de contaminantes criterio y un ajuste contextual activo, por lo que complementa la evidencia real con un caso de referencia donde el sistema recorre toda la secuencia prevista por el diseño. Elaboración propia.}
\label{fig:ui_traceability_mock_controlled}
\end{figure}

El modal de \textit{Explicabilidad} documentó el comportamiento interno del motor \textit{Mamdani}. En la corrida real de Ciudad de México, dominada por O$_3$, la Figura~\ref{fig:ui_explainability_rules_mexico_real} muestra la distribución de reglas activadas. La Figura~\ref{fig:ui_explainability_aggregation_mexico_real} presenta la agregación y la defuzzificación de ese mismo episodio. La Figura~\ref{fig:ui_explainability_aggregation_mock_controlled} añade la lectura controlada del escenario \textit{urban\_escalation}. Ese caso no sustituye a la evidencia real; sirve para mostrar el comportamiento del motor cuando la entrada empuja el sistema hacia una salida más severa y, por tanto, facilita leer la transición entre la base normativa y la puntuación difusa.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_rules_mexico_real.png}
\caption{Distribución de reglas activadas en la corrida real de \textit{Hospital General de México}. La figura evidencia un patrón de activación coherente con un episodio gaseoso de baja severidad, dominado por O$_3$, y permite observar cómo la base de reglas reduce la complejidad de entrada a un conjunto trazable de disparos efectivos. Elaboración propia.}
\label{fig:ui_explainability_rules_mexico_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_aggregation_mexico_real.png}
\caption{Agregación y defuzzificación de la corrida real de \textit{Hospital General de México}. La superficie agregada y la puntuación resultante muestran cómo el motor \textit{Mamdani} transforma la evidencia normativa y auxiliar en una salida crisp sin perder interpretabilidad del proceso intermedio. Elaboración propia.}
\label{fig:ui_explainability_aggregation_mexico_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_aggregation_mock_controlled.png}
\caption{Agregación y defuzzificación del escenario controlado \textit{urban\_escalation}. La curva muestra la respuesta del motor bajo una entrada diseñada para producir una alerta superior y complementar los casos reales que, en esta iteración, no alcanzaron ese nivel de severidad. Elaboración propia.}
\label{fig:ui_explainability_aggregation_mock_controlled}
\end{figure}

Las Figuras~\ref{fig:ui_explainability_aqi_santiago_real} y~\ref{fig:ui_explainability_concurrence_santiago_real} trasladan el análisis a Santiago. La primera sitúa el AQI consolidado dentro de las categorías lingüísticas del antecedente normativo. La segunda muestra cómo el sistema representa la concurrencia de contaminantes soportados. La Figura~\ref{fig:ui_explainability_persistence_lima_real} completa la lectura de variables auxiliares con el caso de Lima y permite observar la persistencia temporal del episodio.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_membership_aqi_santiago_real.png}
\caption{Funciones de pertenencia del AQI para la corrida real de \textit{Parque O'Higgins}. La figura sitúa el AQI consolidado dentro de las categorías lingüísticas del antecedente normativo y ayuda a justificar por qué el episodio queda ubicado en una zona moderada del sistema difuso. Elaboración propia.}
\label{fig:ui_explainability_aqi_santiago_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_membership_concurrence_santiago_real.png}
\caption{Funciones de pertenencia de concurrencia para la corrida real de \textit{Parque O'Higgins}. La visualización muestra cómo el artefacto representa la coexistencia efectiva de contaminantes soportados y la incorpora como variable auxiliar en la inferencia principal. Elaboración propia.}
\label{fig:ui_explainability_concurrence_santiago_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_explainability_membership_persistence_lima_real.png}
\caption{Funciones de pertenencia de persistencia para la corrida real de \textit{SAN BORJA}. La captura muestra cómo la continuidad temporal del episodio se traduce a términos lingüísticos antes de combinarse con el AQI y la concurrencia en la base principal de reglas. Elaboración propia.}
\label{fig:ui_explainability_persistence_lima_real}
\end{figure}

El modal de \textit{Evaluación} cerró la validación con cuatro lecturas distintas. La Figura~\ref{fig:ui_evaluation_context_lima_real} corresponde a una corrida real de Lima y muestra que la capa contextual fue evaluada sin modificar la severidad base, lo que era coherente con la señal observada. La Figura~\ref{fig:ui_evaluation_context_mock_controlled} usa \textit{urban\_escalation} y muestra el caso opuesto: la capa contextual sí altera la salida final. La Figura~\ref{fig:ui_evaluation_history_lima_real} recupera una comparación histórica real entre episodios observados por el backend y deja ver que el histórico local ya permite contrastar AQI, cobertura y puntuación difusa entre corridas de estaciones distintas. La Figura~\ref{fig:ui_evaluation_history_mock_controlled} añade la comparación del escenario controlado actual con una corrida histórica previa del mismo escenario, lo que permite ver la estabilidad de la respuesta cuando la entrada se mantiene controlada.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_evaluation_context_lima_real.png}
\caption{Comparación entre salida principal y salida final en la corrida real de \textit{SAN BORJA}. La coincidencia entre ambas barras indica que la capa contextual fue evaluada, pero no encontró evidencia suficiente para modificar la severidad base del episodio. Elaboración propia.}
\label{fig:ui_evaluation_context_lima_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_evaluation_context_mock_controlled.png}
\caption{Comparación entre salida principal y salida final en el escenario controlado \textit{urban\_escalation}. La diferencia entre ambas barras muestra el efecto explícito del ajuste contextual cuando temperatura y humedad empujan la salida hacia una categoría más severa. Elaboración propia.}
\label{fig:ui_evaluation_context_mock_controlled}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_evaluation_history_lima_real.png}
\caption{Comparación entre la corrida actual de \textit{SAN BORJA} y una corrida histórica real de \textit{Parque O'Higgins}. La vista confirma que el histórico local ya permite contrastar episodios registrados por el backend y utilizar esa memoria como apoyo para análisis posteriores. Elaboración propia.}
\label{fig:ui_evaluation_history_lima_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{propuesta/ui_evaluation_history_mock_controlled.png}
\caption{Comparación entre la corrida actual de \textit{urban\_escalation} y una corrida histórica previa del mismo escenario. La vista muestra que el histórico local también permite contrastar escenarios controlados y verificar estabilidad de salida cuando la entrada se repite bajo las mismas condiciones. Elaboración propia.}
\label{fig:ui_evaluation_history_mock_controlled}
\end{figure}

La evidencia reunida permitió verificar cuatro hechos. El primero es que la interfaz conserva la semántica técnica de la salida JSON y la hace auditable por capas. El segundo es que las estaciones reales producen comportamientos distintos y útiles para contrastar episodios gaseosos y particulados. El tercero es que el escenario controlado sigue siendo necesario para mostrar una alerta más severa y un ajuste contextual activo, porque esa combinación no apareció en las estaciones consultadas. El cuarto es que el frontend ya puede exportar cada gráfica de \textit{Chart.js} como PNG directamente desde el panel correspondiente, lo que facilita documentación, revisión y reutilización de evidencia visual sin depender de capturas manuales del navegador.

La integración con \textit{OpenAQ} quedó validada a nivel de código, configuración y cliente de consulta. El prototipo recupera estaciones por \textit{location\_id}, consulta sensores asociados y construye series horarias por parámetro. Las corridas reales también hicieron visible una limitación importante: no toda estación con sensores publicados produce una ventana útil para el modelo. Un ejemplo es \textit{Adrogué} en Buenos Aires (\texttt{2847298}), cuya ficha expone PM$_{2.5}$, PM$_{10}$, temperatura y humedad relativa en \textit{OpenAQ}, pero cuya ventana consultada devolvió series vacías, cobertura global nula y una categoría \textit{sin\_datos}. Esa restricción forma parte del comportamiento real del sistema sobre datos abiertos vivos y debe conservarse explícitamente en la evaluación.

En el estado actual del trabajo, el resultado principal no es todavía una comparación extensa de desempeño entre múltiples estaciones. El resultado alcanzado es la construcción de un artefacto ejecutable, trazable y explicable, con base normativa explícita, base principal de 54 reglas, capa contextual de 9 reglas, API HTTP, despliegue en contenedores e interfaz web inicial lista para etapas posteriores de validación comparativa y consolidación operativa.
