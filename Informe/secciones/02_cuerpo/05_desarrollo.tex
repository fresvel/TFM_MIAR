% DESARROLLO DEL PROYECTO Y RESULTADOS

\section{Desarrollo del proyecto y resultados}



\subsection{Planteamiento del problema}

La calidad del aire constituye un problema técnico, sanitario y de gestión pública cuya complejidad supera la mera medición puntual de contaminantes. La evidencia reciente muestra que la exposición prolongada a contaminantes atmosféricos sigue asociándose a una carga global elevada de enfermedad y mortalidad: distintos antecedentes reportan alrededor de siete millones de muertes a escala mundial vinculadas a la exposición prolongada a material particulado y otros contaminantes del aire \cite[p.~1]{ART07}, mientras que otros trabajos sitúan en 4.2 millones las muertes prematuras asociadas a contaminación atmosférica exterior \cite[p.~1]{ART04}. Esta magnitud confirma que el problema no se limita a la vigilancia ambiental, sino que exige mecanismos de evaluación y alerta que puedan traducir datos técnicos en decisiones operativas oportunas para reducir exposición y riesgo.

Dentro de ese escenario, el índice de calidad del aire (AQI) se ha consolidado como uno de los principales instrumentos para interpretar el estado del aire y comunicarlo a autoridades y ciudadanía. Sin embargo, la literatura especializada también señala que persisten diferencias relevantes en contaminantes considerados, métodos de cálculo, escalas de agregación y criterios de interpretación entre modelos de AQI. La revisión crítica de modelos AQI desarrollados entre 1960 y 2021 concluye, de forma explícita, que todavía se requiere un modelo más confiable, extensible y comparable para apoyar programas estratégicos de abatimiento de la contaminación y preservar la salud pública \cite[p.~1]{ART01}. Esta necesidad se vuelve más evidente en contextos urbanos con alta presión ambiental; por ejemplo, en dicha revisión se reporta que Delhi registró en 2019 una concentración media diaria de PM$_{2.5}$ de 98.6~\(\mu g/m^3\), equivalente a diez veces la recomendación de la OMS \cite[p.~2]{ART01}. En consecuencia, el problema no es únicamente captar datos, sino convertirlos en evaluaciones consistentes, comparables y útiles para la toma de decisiones.

En respuesta a esta necesidad, los trabajos recientes han incorporado lógica difusa, IoT y arquitecturas distribuidas para clasificación, monitoreo y predicción de calidad del aire. No obstante, el análisis de antecedentes muestra que estos avances siguen siendo parciales y heterogéneos. Existen propuestas basadas en datasets abiertos que reconocen la necesidad de ampliar su validación a otras localizaciones para reducir sesgo geográfico \cite[p.~7]{ART04}; otras implementaciones priorizan el monitoreo y control local, pero declaran que no generaron ni analizaron datasets para validar su comportamiento de manera reproducible \cite[p.~10]{ART05}; y trabajos centrados en redes WSN mejoran eficiencia de red, retardo o entrega de paquetes, pero concentran la optimización en la infraestructura de transmisión más que en la validación comparable de modelos predictivos sobre datos reutilizables \cite[pp.~1,4]{ART08}. Esta diversidad confirma que el campo está activo, pero también que la trazabilidad del dato, la comparabilidad de evaluación y la transferencia de resultados entre contextos siguen siendo insuficientes.

La revisión sistemática desarrollada en este trabajo refuerza esa misma lectura. En el corpus analizado predominan estudios sustentados en adquisición directa por sensores, mientras la utilización exclusiva de datos públicos es excepcional; además, la mayor parte de las propuestas trabaja con conjuntos reducidos de contaminantes y con énfasis mayor en clasificación o alertamiento, no en predicción robusta y externamente comparable. Los estudios orientados a predicción, aunque técnicamente prometedores, continúan concentrados en escenarios acotados y con validaciones parciales. Por ejemplo, algunos trabajos combinan datos de pocos sitios urbanos y rurales con validación adicional sobre subconjuntos de datasets de referencia \cite[pp.~3--4]{A135}, \cite[p.~2]{A144}; otros reportan predicción a 24 horas, pero dentro de entornos interiores específicos \cite[p.~1]{A157}; y propuestas recientes con alto ajuste estadístico plantean como trabajo futuro la colaboración entre múltiples ciudades, lo que evidencia que la validación interurbana sigue sin resolverse de manera sólida \cite[p.~5]{A028}. En contraste, casos como \cite[p.~2]{A084} muestran el valor de entrenar y depurar modelos neuro-difusos sobre datos multi-localización del mundo real, pero este tipo de aproximación todavía no representa la práctica dominante.

En funciones metodológicas y de ingeniería, el problema central radica en la ausencia de una solución que integre, en un mismo marco reproducible, cuatro componentes que suelen aparecer desacoplados: una fuente de datos abiertos trazable y suficientemente robusta, un proceso de preparación y control de calidad de datos documentado, un núcleo de inferencia difusa auditable para interpretar riesgo, y una validación comparable que permita verificar estabilidad del desempeño fuera del contexto inicial. Cuando alguno de estos componentes falta, el resultado puede ser útil como prototipo local, pero no como base confiable para una solución transferible y reutilizable en otros escenarios urbanos.

Con base en ello, el problema que aborda este TFM se define como la falta de un módulo de monitoreo de calidad del aire que, apoyado en datos abiertos de redes de monitoreo ambiental, permita evaluar riesgo y generar alertas mediante inferencia difusa bajo un esquema técnicamente trazable, reproducible y con capacidad de validación en escenarios urbanos. Esta formulación se alinea de manera directa con los objetivos del trabajo: caracterizar la evidencia disponible, diseñar una arquitectura funcional coherente con los vacíos detectados, implementar el módulo propuesto e introducir una validación que considere la utilidad práctica del modelo como sistema de apoyo a la decisión.


\subsection{Metodología}

\subsubsection{Enfoque y tipo de investigación}
El presente trabajo se enmarca en una investigación aplicada, orientada al desarrollo de una solución tecnológica para un problema real. El enfoque metodológico combina, en primer lugar, una revisión sistemática de la literatura para fundamentar el problema y derivar criterios de diseño y, en segundo lugar, una lógica de investigación basada en diseño de artefacto, consistente con los principios de \emph{Design Science Research}, en la que el conocimiento se materializa mediante la definición, construcción y evaluación de una solución útil frente a una necesidad identificada \cite[pp.~75--77]{METDSR01}, \cite[pp.~46,55--56]{METDSRM01}. En consecuencia, la validación del módulo no se plantea como un estudio experimental clásico, sino como una evaluación empírica del artefacto centrada en la utilidad, calidad y desempeño de la solución desarrollada.

\subsubsection{Diseño metodológico del estudio}
El estudio se organiza en fases metodológicas claramente definidas, con el fin de garantizar coherencia y trazabilidad entre los objetivos planteados y las actividades desarrolladas. Esta secuencia no replica de forma estricta un modelo único, pero se apoya en la articulación entre el proceso de revisión sistemática y la lógica nominal propuesta por DSRM para investigación basada en diseño \cite[pp.~46,71]{METDSRM01}. Bajo ese criterio, la primera fase cumple la función de fundamentación y delimitación del problema, mientras que las fases posteriores condensan operativamente la definición de la solución, la construcción del artefacto, su demostración funcional y su evaluación. Las fases del estudio son las siguientes:

\begin{enumerate}
  \item Revisión sistemática del estado del arte.
  \item Diseño del módulo de monitoreo.
  \item Implementación del módulo propuesto.
  \item Validación del módulo propuesto.
\end{enumerate}

Este apartado actúa como un mapa metodológico del trabajo.

\subsubsection{1: Revisión sistemática del estado del arte}


La revisión sistemática del estado del arte constituye la primera fase metodológica de este TFM. Aunque su desarrollo forma parte del presente trabajo, la revisión se ejecutó como un estudio independiente y su versión completa se consolidó en un manuscrito externo sometido a publicación. En esta memoria, la fase 1 se presenta desde su función metodológica: delimitar el problema, fundamentar el estado del arte y derivar criterios de diseño para la propuesta.

La revisión sistemática desarrollada en esta fase se estructura mediante el modelo \textbf{PICo}, con el fin de delimitar de manera precisa el fenómeno de estudio, el enfoque metodológico y el contexto de aplicación, garantizando coherencia con el carácter aplicado de la investigación.

\input{assets/tablas/02_cuerpo/05_desarrollo/pico_air_quality.tex}


Este enfoque permite identificar y analizar de manera sistemática las contribuciones existentes que combinan evaluación de la calidad del aire, lógica difusa y plataformas de monitoreo en tiempo real, estableciendo la base conceptual y técnica para el desarrollo de la propuesta planteada en este trabajo.


A partir de la delimitación establecida mediante el modelo PICo, se formula la siguiente pregunta de investigación, que orienta el análisis del estado del arte y fundamenta el desarrollo de la propuesta técnica del presente trabajo.

\textbf{Pregunta de investigación:}

\begin{quote}
¿Cómo se utilizan los sistemas de inferencia basados en lógica difusa en plataformas de monitoreo en tiempo real para evaluar la calidad del aire urbano y los riesgos asociados?
\end{quote}

Con el fin de operacionalizar esta pregunta y estructurar de manera sistemática la revisión del estado del arte, se definen las siguientes directrices de investigación:

\begin{itemize}
    \item \textbf{Directriz 1 (Datos y variables):}  
    ¿Qué contaminantes atmosféricos y variables ambientales se utilizan con mayor frecuencia para evaluar la calidad del aire urbano en plataformas de monitoreo en tiempo real?

    \item \textbf{Directriz 2 (Enfoque de lógica difusa):}  
    ¿Qué tipos de sistemas de inferencia basados en lógica difusa se emplean para la evaluación de la calidad del aire y del riesgo asociado, y cuáles son sus principales criterios de diseño?

    \item \textbf{Directriz 3 (Plataforma y tiempo real):}  
    ¿Cómo se implementan las plataformas de monitoreo en tiempo real para el análisis y visualización de la calidad del aire, y qué arquitecturas y tecnologías se emplean?
\end{itemize}


\subsubsection*{Estrategia de búsqueda}


% =======================================================
% Estrategia de búsqueda y construcción de la cadena final
% =======================================================

La búsqueda bibliográfica se realizó en las bases de datos Scopus, Web of Science e IEEE Xplore, seleccionadas por su amplia cobertura en ingeniería, sistemas inteligentes y ciencias ambientales. A partir de estas fuentes, se definió la estrategia de búsqueda con base en el marco PICo previamente establecido, con el objetivo de identificar literatura relevante sobre el uso de sistemas de inferencia basados en lógica difusa en plataformas de monitoreo de calidad del aire en tiempo real. La búsqueda se restringió a publicaciones en idioma inglés, publicadas dentro del período 2021--2026, e incluyó artículos de revista y actas de congresos revisados por pares.

Para la construcción de la cadena de búsqueda, se identificaron los conceptos nucleares de cada componente PICo (Problema/Fenómeno, Interés y Contexto) y se derivaron términos variantes y expresiones equivalentes empleadas de forma recurrente en la literatura científica. Estos términos se organizaron en bloques conceptuales independientes, en los cuales se establecieron términos ancla mediante operadores lógicos AND y variantes semánticas mediante operadores OR, permitiendo controlar la expansión de la búsqueda y evitar redundancias semánticas. La Tabla~\ref{tab:derivacion_pico} presenta los términos derivados para cada componente del marco PICo, los cuales sirvieron como base para la construcción de los bloques de búsqueda.

\input{assets/tablas/02_cuerpo/05_desarrollo/derivacion_pico.tex}

Las cadenas resultantes fueron ajustadas en las bases de datos seleccionadas mediante la evaluación de la pertinencia de los estudios recuperados a partir del análisis de títulos y resúmenes, hasta obtener una formulación final con cobertura temática adecuada y un volumen de resultados manejable para la revisión sistemática de la literatura.

Con base en los términos derivados, se construyeron bloques lógicos correspondientes a cada componente PICo. En el componente P, se exigió la presencia de términos asociados al medio atmosférico junto con indicadores de calidad del aire o contaminantes específicos. En el componente I, se combinaron términos que identifican explícitamente enfoques de lógica difusa y sistemas de inferencia, incluyendo variantes híbridas ampliamente utilizadas en el dominio. En el componente Co, se estableció un anclaje simultáneo al tiempo real, a la noción de sistema o plataforma, y a la instrumentación basada en sensores, garantizando que los estudios recuperados correspondan a entornos de monitoreo operativo.

Los bloques correspondientes a los componentes P, I y Co se integraron en una única cadena de búsqueda mediante el operador lógico AND, de modo que los estudios recuperados cumplieran simultáneamente con los tres componentes del marco PICo. Como resultado de este proceso de refinamiento, se obtuvo la siguiente cadena de búsqueda final, utilizada para la recuperación de estudios en las bases de datos seleccionadas:

\input{assets/tablas/02_cuerpo/05_desarrollo/cadena_final.tex}

\noindent
Esta cadena representa un compromiso metodológico entre exhaustividad y precisión, y permitió recuperar un conjunto de estudios suficientemente amplio para caracterizar el estado del arte, pero acotado para posibilitar un análisis profundo y sistemático en el marco del presente trabajo.




\subsubsection*{Selección de Artículos}

El proceso de selección se ejecutó conforme al flujo PRISMA 2020 \cite{prisma2020}, articulando de forma secuencial las fases de identificación, cribado, obtención de texto completo, elegibilidad e inclusión final. Los criterios operacionales de inclusión y exclusión utilizados en las fases de cribado y elegibilidad se presentan en la Tabla~\ref{tab:criterios_ie}. Tras la consolidación de resultados provenientes de las bases consultadas y la depuración de duplicados, se conformó el conjunto de registros a cribar por título y resumen.
Para fines de trazabilidad y reproducibilidad, los artefactos finales de resultados y respaldo metodológico se publican en un repositorio único en Zenodo.

\input{assets/tablas/02_cuerpo/05_desarrollo/criterios_ie.tex}

El cribado de título y resumen se aplicó de manera sistemática al conjunto de registros identificados tras la depuración de duplicados, evaluando para cada registro la relación con el fenómeno de calidad del aire (P), el uso central de lógica difusa (I), la evaluación de calidad del aire mediante lógica difusa (P+I), la naturaleza del estudio y la existencia de plataforma o sistema de monitoreo (Co). Esta fase permitió separar los estudios excluidos de aquellos que debían pasar a recuperación de texto completo.

La obtención de textos completos combinó una estrategia automatizada y una estrategia manual. En la fase automatizada, cada registro con DOI se consultó primero en Unpaywall y, cuando no se identificó un PDF directo, se aplicó una segunda consulta en OpenAlex para localizar enlaces de acceso abierto. Esta etapa permitió clasificar de forma trazable los casos descargados y los no recuperables por ausencia de DOI, falta de enlace PDF directo o restricción de acceso. Para los casos no obtenidos automáticamente, se realizó búsqueda manual en las plataformas de las bases indexadas, en arXiv y en ResearchGate; cuando el documento no estuvo disponible en acceso directo, se gestionó la solicitud a los autores mediante ResearchGate. El estado de recuperación se documentó en la matriz de cribado a través de \texttt{Retrieval\_result}.

La elegibilidad se evaluó sobre los estudios recuperados en texto completo, aplicando los criterios D1--D3 y el criterio transversal de diseño metodológico y validación, con registro explícito de evidencia por página. En esta etapa se consignó una razón única de exclusión por estudio cuando correspondía, manteniendo consistencia con el reporte PRISMA de exclusiones en texto completo. El detalle cuantitativo por fase se presenta en la sección de resultados y en el diagrama PRISMA, preservando aquí el foco metodológico del procedimiento.

%%%%%%%%%%%%%%%%%%%%%%%Evaluación de Calidad%%%%%%%%%%%%%%%%%%%%

\subsubsection{Evaluación de Calidad}

La evaluación de calidad se fundamentó en el apartado 6.3 de Kitchenham (2007) \cite{kitchenham2007}, aplicando su recomendación de seleccionar los ítems en función de las preguntas de investigación. Cada pregunta del checklist fue contrastada con la pregunta principal de la revisión y con las directrices D1--D3, conservando únicamente aquellos criterios que aportaban evidencia relevante sobre validez metodológica y robustez de resultados en el contexto de monitoreo de calidad del aire con lógica difusa.

El procedimiento incluyó una tipificación previa de los estudios. Cada artículo incluido por texto completo se clasificó como \texttt{Qualitative}, \texttt{Quantitative} o \texttt{Mixed}. Para los estudios con componente cuantitativo, se realizó una subclasificación en: cuantitativo empírico general, correlacional/observacional, \textit{survey} o experimento. El checklist se aplicó de manera condicionada a esta tipificación, evaluando cada estudio solo con los ítems pertinentes a su tipo y subtipo.

El instrumento final se construyó a partir del bloque cuantitativo de Kitchenham \cite{kitchenham2007} (preguntas comunes y específicas por subtipo), aplicando tres criterios de selección: relevancia para la pregunta de investigación, capacidad de identificar riesgo de sesgo (diseño, ejecución, análisis y conclusiones) y aplicabilidad al corpus final. El resultado fue un instrumento de 30 ítems, organizados en un núcleo común y componentes específicos para estudios experimentales, correlacionales/observacionales y cuantitativos empíricos sin subtipo específico. Los ítems asociados a \textit{survey} se excluyeron, ya que no hubo estudios de ese tipo en la muestra.
El checklist completo del instrumento se presenta en el Anexo \ref{ane:anexo_checklist_calidad}, Tabla \ref{tab:anexo_checklist_calidad}.
La aplicación ítem por ítem del checklist, junto con su evidencia de soporte, se registró para cada estudio incluido en la matriz de evaluación de calidad.

La unidad de evaluación fue el estudio primario incluido por texto completo. Dado que el corpus final estuvo compuesto exclusivamente por estudios cuantitativos, no se aplicó el checklist cualitativo. La codificación utilizó la escala \texttt{Yes}, \texttt{Partial}, \texttt{No}, \texttt{Unclear} y \texttt{N/A}. La categoría \texttt{Unclear} se empleó cuando la información reportada era insuficiente para decidir, y \texttt{N/A} únicamente cuando el criterio no aplicaba al subtipo del estudio. Cada decisión se respaldó con evidencia textual y referencia de página (formato \texttt{p.X} o \texttt{p.X-p.Y}) para garantizar trazabilidad.

La puntuación agregada se calculó mediante ponderación explícita (\texttt{Yes}=1, \texttt{Partial}=0.5, \texttt{Unclear}=0.25, \texttt{No}=0), excluyendo los \texttt{N/A} del denominador:

\begin{equation}
\textit{Score}_{raw} = 1\cdot n_{Yes} + 0.5\cdot n_{Partial} + 0.25\cdot n_{Unclear}
\end{equation}

\begin{equation}
\textit{Items}_{aplicables} = 30 - n_{N/A}
\end{equation}

\begin{equation}
\textit{Score}_{\%} = \left( \frac{\textit{Score}_{raw}}{\textit{Items}_{aplicables}} \right)\times 100
\end{equation}

Con base en este porcentaje se definieron tres niveles de calidad: \textbf{Alta} (\(\textit{Score}_{\%}\geq 80\)), \textbf{Media} (\(60 \leq \textit{Score}_{\%} < 80\)) y \textbf{Baja} (\(\textit{Score}_{\%}<60\)). La evaluación no se utilizó como criterio de exclusión automática, sino como insumo para interpretar la heterogeneidad de los hallazgos, identificar posibles sesgos y realizar análisis de sensibilidad. De este modo, la calidad orienta la interpretación de la evidencia sin reemplazar el análisis sustantivo de los resultados.




%%%%%%%%%%%%%%%%%%%%%%%%%%%%Extracción de Datos%%%%%%%%%%%%%%%%%%%%%
\subsubsection{Extracción de datos}

La extracción de datos se diseñó para responder de forma directa la pregunta principal de investigación y las directrices D1--D3, manteniendo trazabilidad completa desde cada afirmación de síntesis hasta su evidencia en texto completo. El diseño del instrumento tomó como base el principio de Kitchenham \cite{kitchenham2007} de definir previamente qué datos son necesarios para contestar las preguntas de investigación y cómo deben codificarse para reducir sesgo de interpretación entre estudios.

Antes de la extracción definitiva se ejecutó un piloto sobre un subconjunto de artículos incluidos, con el objetivo de estabilizar definiciones operacionales, reducir ambigüedad semántica y verificar que cada campo aportara valor analítico real. Ese piloto permitió consolidar una matriz operativa estructurada en seis bloques: metadatos bibliográficos, datos y variables (D1), enfoque de lógica difusa (D2), plataforma y operación en tiempo real (D3), resultados de evaluación y trazabilidad metodológica. Como resultado de esta depuración, la matriz final quedó en 48 columnas, evitando campos redundantes y conservando únicamente variables útiles para síntesis transversal.

La ejecución de la extracción se realizó artículo por artículo y criterio por criterio, registrando tanto variables categóricas como descripciones técnicas y valores cuantitativos reportados. Para las variables categóricas se aplicó un catálogo controlado con vocabulario predefinido y reglas de normalización. Cuando una categoría no estaba cubierta por el catálogo, se usó el formato \texttt{Other:<texto>} para preservar precisión sin perder consistencia. El valor \texttt{NR} se reservó para información no reportada y \texttt{NA} para no aplicabilidad metodológica. Esta convención permitió separar ausencia de reporte, no aplicabilidad y variación terminológica genuina.

Toda celda analítica de D1, D2, D3 y evaluación se respaldó con evidencia de página en formato \texttt{p.X} o \texttt{p.X-p.Y}. La evidencia se registró de forma independiente por bloque (\texttt{Evidence\_D1\_pages}, \texttt{Evidence\_D2\_pages}, \texttt{Evidence\_D3\_pages}, \texttt{Evidence\_Eval\_pages}) para facilitar auditoría focalizada y revisión cruzada durante la síntesis. Esta estrategia evitó depender de notas narrativas no trazables y mejoró la reproducibilidad del proceso.

La consistencia de la extracción se verificó en dos niveles. Primero, se aplicaron controles estructurales y semánticos sobre la matriz completa: completitud de campos críticos, conformidad con catálogo, formato de evidencia y coherencia entre columnas relacionadas. Segundo, se ejecutó una auditoría de veracidad contra PDFs para confirmar que las páginas citadas efectivamente sustentaran contaminantes, diseño difuso, arquitectura/plataforma y métricas reportadas. Con estos controles, la matriz final de extracción quedó validada para los 19 estudios incluidos en texto completo.

La implementación operativa de esta fase se conserva en la matriz final de extracción y en su catálogo de codificación controlada. La vista conceptual de la matriz se resume en la Tabla \ref{tab:matriz_extraccion_actualizada} y el detalle campo a campo se documenta en el Anexo \ref{ane:anexo_diccionario_extraccion}.


\input{assets/tablas/02_cuerpo/05_desarrollo/extraccion_de_datos.tex}


\subsubsection{Análisis y Síntesis de Datos}
El análisis y la síntesis de datos se desarrollaron con un enfoque estructurado y reproducible, alineado con Kitchenham, y organizado en cinco fases analíticas encadenadas ilustradas en la Figura \ref{fig:anlisis_sintesis}. En la primera fase (preparación analítica), se trabajó sobre la matriz consolidada de extracción de datos, verificando consistencia entre identificadores, campos analíticos y evidencia por estudio. En esta etapa, se aplicó una estandarización a las columnas categóricas y multietiqueta mediante normalización semántica, descomposición de valores compuestos y armonización de variantes terminológicas, preservando siempre la trazabilidad con el estudio de origen.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{metodología/analisis_sintesis.png}
\caption{Fases del análisis y síntesis de datos. Elaboración propia.}
\label{fig:anlisis_sintesis}
\end{figure}


En la segunda fase (evidencia cuantitativa y visual), se construyó la base descriptiva mediante frecuencias absolutas y relativas (\(k/n\), \%) y tablas de distribución por dimensión analítica, complementadas con gráficos de barras de frecuencia por categoría, gráficos de distribución del número de variables por estudio y figuras compuestas con subfiguras comparativas.

En la tercera fase (identificación de patrones), el análisis pasó de describir categorías a relacionarlas entre sí. Para ello se utilizaron tablas cruzadas y matrices de coocurrencia, con el objetivo de responder cómo se combinan las decisiones metodológicas dentro de los estudios y no solo qué tan frecuentes son de forma aislada. En este punto se identificaron convergencias, configuraciones recurrentes y variaciones entre enfoques. Las columnas descriptivas no se forzaron a categorías cerradas; se usaron como apoyo contextual para interpretar por qué ciertos patrones aparecen y cómo deben leerse en la síntesis narrativa.

En la cuarta fase (construcción de perfiles), los patrones relacionales se consolidaron en perfiles compactos de configuración. Estos perfiles resumen combinaciones típicas de diseño (por ejemplo, entradas, enfoque metodológico o componentes de plataforma) y permiten convertir hallazgos del estado del arte en criterios reutilizables para el diseño del módulo propuesto.

En la quinta fase (integración y redacción de la síntesis), se articularon todos los hallazgos en una narrativa continua y trazable. La redacción se orientó a explicar implicaciones metodológicas y de diseño, evitando listados de categorías sin interpretación y manteniendo vínculo explícito entre evidencia, visualización e inferencia analítica.

La implementación se realizó con Python, utilizando principalmente \texttt{pandas} para transformación y agregación de datos, y \texttt{seaborn}/\texttt{matplotlib} para la generación de visualizaciones analíticas. Este flujo se mantuvo de forma homogénea en las directrices de síntesis y en los bloques de evaluación transversal, garantizando consistencia entre procesamiento, evidencia y redacción final.




\subsubsection{2: Diseño del módulo de monitoreo}
Esta fase se sustenta en la lógica de \emph{Design Science Research}, particularmente en la definición de los objetivos de la solución y en el diseño del artefacto \cite[pp.~55--56]{METDSRM01}. En este punto, el problema delimitado por la revisión sistemática deja de tratarse solo como un hallazgo teórico y se transforma en una propuesta técnica estructurada. Metodológicamente, esto implica traducir la necesidad identificada en requisitos de solución, criterios de funcionamiento y condiciones de uso, de modo que el diseño del módulo responda de forma explícita a los vacíos detectados en la literatura. En coherencia con la SLR y con los objetivos del estudio, los criterios rectores de esta fase son: uso de datos abiertos como base de entrada, trazabilidad del flujo de procesamiento, interpretabilidad del razonamiento difuso y capacidad del módulo para producir resultados verificables y útiles para la evaluación del riesgo.

La traducción de estos criterios a decisiones de diseño se apoya de forma directa en la síntesis conceptual del capítulo anterior. En particular, el módulo se concibe a partir de cuatro fundamentos: el AQI como mecanismo de agregación e interpretación del riesgo; la selección explícita de contaminantes y variables ambientales con significado operativo; la inferencia difusa como núcleo auditable de decisión; y el uso de fuentes abiertas con trazabilidad suficiente para sostener reproducibilidad y validación posterior. Bajo este encuadre, el diseño no se formula como una arquitectura abstracta, sino como una respuesta concreta a los vacíos identificados en comparabilidad, integración operativa y generalización externa.

Por esa razón, las decisiones principales de esta fase deben resolver, desde el diseño, aspectos que la literatura suele dejar fragmentados: declaración de la fuente y ventana temporal de los datos, criterio de selección de variables, definición del esquema categorial de riesgo, papel del motor difuso dentro del pipeline, y forma en que la salida del sistema se convierte en monitoreo interpretable y alertamiento útil. Este anclaje asegura continuidad entre el estado del arte sintetizado, la base conceptual y la arquitectura funcional de la propuesta.

En términos operativos, esta fase se desarrolla en una secuencia de definición progresiva del módulo:
\begin{enumerate}
\item Se establece la arquitectura general de la solución, delimitando sus capas, componentes y relaciones funcionales, con el fin de definir cómo se conectan la adquisición de datos, el procesamiento, la inferencia y la salida del sistema;
\item Se define el flujo de datos, desde la fuente de entrada hasta la generación de la salida final del sistema, especificando qué datos ingresan, cómo se transforman y en qué punto se convierten en una evaluación interpretable;
\item Se precisan los componentes encargados de la ingesta, preparación y transformación de los datos que alimentan el módulo, de modo que el uso de datos abiertos quede resuelto como una decisión metodológica y no solo como una elección técnica posterior;
\item Se determina el papel del motor de inferencia difusa dentro del proceso, especificando cómo interviene en la evaluación del riesgo y en la generación de alertas, y dejando claro qué parte de la decisión depende de reglas interpretables derivadas del conocimiento experto;
\item Se fijan los criterios de diseño que orientarán la implementación posterior, entendidos como condiciones que la propuesta debe cumplir: trazabilidad del procesamiento para poder auditar el recorrido de los datos, interpretabilidad de las reglas difusas para justificar la salida del sistema y evaluabilidad mediante métricas verificables para comprobar el cumplimiento de los objetivos.
\end{enumerate}

Con esta secuencia, la fase 2 cumple la función de convertir la evidencia previa en una solución viable, coherente y técnicamente justificable, en línea con la noción de artefacto propuesta por Hevner et al. \cite[pp.~84--85]{METDSR01}.

\subsubsection{3: Implementación del módulo propuesto}
Esta fase corresponde a la construcción del artefacto y toma como referencia el bloque de \emph{design and development} de DSRM, entendido como la creación efectiva del objeto diseñado \cite[p.~55]{METDSRM01}. Metodológicamente, aquí la solución deja de ser una formulación de diseño y pasa a expresarse como un sistema implementado. Por ello, la fase se orienta a materializar el artefacto definido previamente, conservando trazabilidad con los objetivos funcionales y con los criterios de diseño establecidos en la fase 2. En coherencia con la SLR y con el objetivo de desarrollar un módulo operable y auditable, la construcción se rige por cuatro criterios: integración funcional, reproducibilidad técnica, consistencia del pipeline y demostración de operabilidad.

La integración funcional implica que los componentes definidos en la fase de diseño deben trabajar como un solo sistema y no como piezas aisladas. La reproducibilidad técnica exige que la implementación pueda volver a ejecutarse bajo las mismas reglas de procesamiento, con los mismos insumos y con resultados consistentes. La consistencia del pipeline supone que la secuencia de adquisición, preparación, inferencia y generación de salida conserve un comportamiento estable entre entradas comparables. Finalmente, la demostración de operabilidad requiere comprobar que el módulo ya puede ejecutarse de extremo a extremo y producir una salida interpretable antes de pasar a la fase de validación.

En términos operativos, esta fase se desarrolla como un proceso de construcción e integración del módulo:
\begin{enumerate}
\item Se seleccionan y configuran las tecnologías, librerías y componentes necesarios para soportar la implementación del sistema, procurando que la base tecnológica permita ejecutar el flujo definido y mantener coherencia con los objetivos funcionales;
\item Se implementa el pipeline de adquisición, depuración y preparación de los datos que alimentan el módulo, asegurando que cada transformación conserve trazabilidad y que el tratamiento de los datos responda a la necesidad de trabajar con insumos abiertos y verificables;
\item Se construye el motor de inferencia difusa conforme a la lógica de diseño establecida, incluyendo sus reglas, variables y mecanismo de salida, de manera que la inferencia mantenga interpretabilidad y correspondencia con el objetivo de evaluar riesgo y emitir alertas;
\item Se integran los distintos componentes del sistema para permitir la ejecución coordinada del flujo completo de procesamiento, verificando que la entrada, la inferencia y la salida queden enlazadas en una operación continua y funcional;
\item Se realiza una demostración funcional inicial del artefacto, verificando que el módulo puede ejecutarse de extremo a extremo y producir una salida interpretable frente a una instancia concreta del problema, como evidencia de factibilidad antes de su evaluación formal.
\end{enumerate}

Esta demostración no sustituye la validación posterior, sino que cumple una función de factibilidad, en el sentido de mostrar que el artefacto ya puede operar como solución implementada. En ese punto, la fase 3 se alinea con la idea de demostración de uso planteada por DSRM y con la noción de \emph{proof by construction} descrita en la literatura de diseño \cite[pp.~55--56]{METDSRM01}, \cite[p.~85]{METDSR01}.

\subsubsection{4: Validación del módulo propuesto}
La validación del módulo se plantea como una evaluación empírica del artefacto, sustentada en el paso de \emph{evaluation} de DSRM y en la guía de evaluación de diseño propuesta por Hevner et al. \cite[p.~56]{METDSRM01}, \cite[pp.~86--87]{METDSR01}. Metodológicamente, esta fase tiene como propósito observar y medir en qué grado el módulo satisface los objetivos definidos durante el diseño, contrastando el comportamiento del sistema implementado con los requisitos funcionales y de desempeño establecidos para la solución. En coherencia con la SLR y con el objetivo de validar el módulo propuesto, la evaluación se organiza a partir de tres criterios: comportamiento funcional, consistencia de la respuesta y desempeño operativo.

El comportamiento funcional se refiere a que el módulo complete el flujo previsto sin interrupciones injustificadas, desde la recepción de datos hasta la emisión del resultado. La consistencia de la respuesta se refiere a que salidas equivalentes o comparables se mantengan alineadas con las reglas definidas en el diseño, evitando respuestas contradictorias frente a condiciones similares. El desempeño operativo se refiere a que el procesamiento ocurra en un tiempo razonable para el propósito de monitoreo y que el sistema mantenga estabilidad básica ante variaciones previsibles en la entrada.

En coherencia con los criterios derivados de la revisión sistemática, la validación se estructura sobre escenarios de uso y métricas que permitan comprobar si el módulo funciona como una herramienta trazable de apoyo a la decisión. Los escenarios de prueba considerados se orientan a verificar el comportamiento del artefacto en tres situaciones: operación nominal con datos dentro de rangos esperados; condiciones con valores cercanos o superiores a umbrales de alerta, para verificar la activación coherente de niveles de riesgo; y casos con valores faltantes, atípicos o con variaciones de calidad en la entrada, para comprobar la robustez básica del flujo de procesamiento. A su vez, la evaluación se apoya en métricas de funcionamiento y desempeño, entre ellas la completitud del procesamiento, entendida como la capacidad del módulo para recorrer todas sus etapas y producir una salida; la consistencia entre entradas y salidas, entendida como la correspondencia lógica entre las condiciones observadas y la respuesta generada; el tiempo de respuesta del módulo, entendido como el lapso requerido para transformar la entrada en un resultado útil; y la coherencia de la clasificación o alertamiento generado respecto de las reglas y criterios definidos en el diseño.

Dado que la propuesta se apoya en datos abiertos y no en una red propia de sensores, la validación no se centra en calibración instrumental, sino en la solidez del flujo de adquisición, preparación, inferencia y publicación sobre series externas. En consecuencia, la comprobación debe asegurar que la fuente seleccionada, la ventana temporal utilizada, el tratamiento de valores faltantes o atípicos y la lógica de categorización del riesgo queden documentados de forma explícita. Este punto es relevante porque la reproducibilidad de la solución depende tanto de la consistencia del motor difuso como de la trazabilidad de los datos que alimentan el módulo.

En términos operativos, la validación se desarrolla mediante una secuencia de comprobación y análisis del comportamiento del módulo:
\begin{enumerate}
\item Se aplican escenarios de prueba que representen condiciones pertinentes de uso del artefacto, incluyendo operación nominal, activación de condiciones de riesgo y verificación del procesamiento ante variaciones en la calidad de los datos de entrada;
\item Se emplean métricas y criterios de evaluación orientados a valorar la completitud del flujo de procesamiento, la consistencia de las salidas, el tiempo de respuesta y la correspondencia entre las alertas generadas y las condiciones detectadas;
\item Se ejecuta el sistema bajo los escenarios definidos, registrando las salidas producidas y el comportamiento observado durante su operación;
\item Se analizan los resultados obtenidos para verificar si el módulo responde de forma coherente con los objetivos planteados en la fase de diseño;
\item Se identifican posibles ajustes o mejoras sobre el artefacto cuando la evidencia obtenida muestre brechas entre el comportamiento esperado y el comportamiento observado.
\end{enumerate}

Con esta secuencia, la fase 4 no se orienta a probar hipótesis experimentales, sino a determinar si el artefacto resulta útil, consistente y técnicamente adecuado frente al problema abordado. En ese sentido, la validación se entiende como un proceso de evaluación del desempeño y de la efectividad de la solución implementada, con capacidad de retroalimentar refinamientos posteriores sobre el módulo cuando sea necesario.

\subsubsection{Consideraciones finales de la metodología}
Finalmente, se presentan consideraciones metodológicas relacionadas con las limitaciones del estudio, su alcance y posibles líneas de trabajo futuro, cerrando la coherencia metodológica del Trabajo Fin de Máster.



\subsection{Desarrollo del proyecto}

\subsection{Resultados}
